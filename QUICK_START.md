# 3D Bin Packing 快速開始指南

## 🚀 快速啟動

### 1. 安裝依賴

```bash
cd 3js/three.js
pip install -r requirements.txt
```

### 2. 啟動所有服務器

```bash
python start_servers.py
```

這將同時啟動：
- RL API服務器 (端口 8888)
- 3D Bin Packing服務器 (端口 8889)  
- HTTP前端服務器 (端口 8000)

### 3. 訪問前端

打開瀏覽器訪問：http://localhost:8000

## 📦 使用3D Bin Packing

### 基本步驟

1. **添加物件**
   - 使用工具列添加立方體、球體、圓柱體等
   - 可以調整物件的大小和位置

2. **執行打包**
   - 點擊右上角的"執行3D打包"按鈕
   - 系統會自動選擇同步或非同步模式

3. **查看結果**
   - 觀察打包後的物件排列
   - 查看體積利用率和執行時間

### 功能說明

- **同步模式**: 物件數量 ≤ 10，立即顯示結果
- **非同步模式**: 物件數量 > 10，顯示進度條
- **可取消任務**: 點擊"取消打包"按鈕

## 🧪 測試功能

### 運行測試

```bash
python test_bin_packing.py
```

### 手動測試API

```bash
# 測試健康檢查
curl http://localhost:8889/health

# 測試同步打包
curl -X POST http://localhost:8889/pack_objects \
  -H "Content-Type: application/json" \
  -d '{
    "objects": [
      {
        "uuid": "test-1",
        "type": "BoxGeometry",
        "position": {"x": 0, "y": 0, "z": 0},
        "scale": {"x": 10, "y": 10, "z": 10},
        "rotation": {"x": 0, "y": 0, "z": 0}
      }
    ],
    "container_size": {"width": 100, "height": 100, "depth": 100},
    "async_mode": false
  }'
```

## 🔧 故障排除

### 常見問題

1. **端口被佔用**
   ```bash
   # 檢查端口使用情況
   netstat -ano | findstr :8888
   netstat -ano | findstr :8889
   ```

2. **依賴安裝失敗**
   ```bash
   # 升級pip
   python -m pip install --upgrade pip
   
   # 重新安裝依賴
   pip install -r requirements.txt --force-reinstall
   ```

3. **前端無法連接後端**
   - 確認所有服務器都已啟動
   - 檢查瀏覽器控制台是否有錯誤
   - 確認CORS設置正確

### 日誌查看

- RL API服務器日誌：查看終端輸出
- 3D Bin Packing服務器日誌：查看終端輸出
- 前端錯誤：打開瀏覽器開發者工具

## 📚 更多信息

- 詳細文檔：`README_3D_BIN_PACKING.md`
- API文檔：訪問 http://localhost:8889
- 算法說明：查看 `src/api_server/bin_packing_api.py`

## 🎯 下一步

1. 嘗試添加更多物件測試打包效果
2. 調整容器尺寸觀察利用率變化
3. 探索不同的算法參數
4. 集成到現有的RL系統中
